---
title: "Move Tool"
description: "Move objects and snap them to other objects"
---

## Using the Move tool

To activate the Move tool press `m` + `spacebar`&#x20;

Once active, select the object you want to move. This sets the point of interest so you can snap a certain point to another object.

The selected object then moves freely, depending on the mouse pointer’s location and camera

Once you're satisfied with placement, click to commit the action.

\[asset]

## Moving objects a predefined distance

To move selected objects a predefined distance, activate the Move tool and enter the number in the  in-scene input field.&#x20;

\[asset]