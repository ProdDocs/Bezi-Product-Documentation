---
title: "Event Types and Input Methods"
description: "Use events to trigger animations throughout a scene. "
---

**\[there's no mention of Input Methods here. should we remove that from the header to avoid confusion?]**

## Using events

Events are actions that trigger animations. Add interactions to objects using the State Machine \[prod doc link] and define events that trigger animations using the Timeline \[prod doc link].&#x20;

## Event types

<AccordionGroup>
  <Accordion title="Auto" defaultOpen={false}>
    Automatically start playing animations when the scene is loaded.
  </Accordion>

  <Accordion title="On pointer down" defaultOpen={false}>
    Trigger the animation when the pointer is pressed.&#x20;

    How the 'On pointer down' event works with different input methods:

    * Mouse: Left click

    * Touch: Tap

    * VR controllers: Index finger triggers (Quest)

    * Hands: Index finger and thumb pinch
  </Accordion>

  <Accordion title="On pointer up" defaultOpen={false}>
    Trigger the animation when the pointer is released.&#x20;

    How the 'On pointer up' event works with different input methods:

    * Mouse: Left click

    * Touch: Tap

    * VR controllers: Index finger triggers (Quest)

    * Hands: Index finger and thumb pinch
  </Accordion>

  <Accordion title="On hover inside" defaultOpen={false}>
    Trigger the animation when the pointer hovers on the object.
  </Accordion>

  <Accordion title="On hover outside" defaultOpen={false}>
    Trigger the animation when the pointer exits the object.
  </Accordion>

  <Accordion title="On key down" defaultOpen={false}>
    Trigger the animation when the viewer presses the assigned key. This event requires the viewer to interact with a keyboard.
  </Accordion>

  <Accordion title="On key up" defaultOpen={false}>
    Event triggers the animation when the viewer releases the assigned key. This event requires the viewer to interact with a keyboard.
  </Accordion>

  <Accordion title="Direct touch" defaultOpen={false} />
</AccordionGroup>

##